{
  "name": "migliori-offerte-italia",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "app",
    "api"
  ],
  "scripts": {
    "dev": "npm run dev --prefix app",
    "dev:swa": "swa start app --run \"npm run dev --prefix app\" --api-location api",
    "build": "npm run build --prefix shared && npm run build --prefix api && npm run build --prefix app",
    "test": "npm run test --prefix api"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^2.0.2"
  }
}